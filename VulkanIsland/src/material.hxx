#pragma once


class Material {
public:

    ;

private:

    // resources
    // topology
    // shader handle
    // rasterization state
    // depth and stencil state
    // blending
    // pipeline layout (descriptor set layout)
};
